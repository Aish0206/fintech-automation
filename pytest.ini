#creating costom markers

[pytest]
addopts = --tracing retain-on-failure --video retain-on-failure --screenshot only-on-failure --headed --slowmo 100
markers = 
    smoke: quick critical-path test
    regression: full detailed test suite